global:
    postgresql:
        auth:
            postgresPassword: password
            username: postgres
            password: password
            database: innercircledb

primary:
    service:
        type: NodePort
        nodePorts:
            postgresql: 30100

#    persistence:
#        size: {{ env "POSTGRES_PV_SIZE" }}

